export const selectRegisterEmail = state => {
  return state.register.email;
};

export const selectRegisterPassword = state => {
  return state.register.password;
};

export const selectRegisterValidateCode = state => {
  return state.register.validateCode;
};

